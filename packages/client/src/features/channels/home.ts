/**
 * Sentinel "channel id" for the Home view, which shows notes from every channel
 * instead of one. It is never a real channel's id (those are uuids or
 * `DEFAULT_CHANNEL_ID`), so selection can be a single string.
 */
export const HOME_ID = "__home__";
